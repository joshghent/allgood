import { healthcheckHandler } from './healthcheck.js';
import { cacheConnection } from './checks/cache.js';
import { cpuCheck } from './checks/cpu.js';
import { dbConnection } from './checks/db.js';
import { disk } from './checks/disk.js';
import { egress } from './checks/egress.js';
import { memoryCheck } from './checks/memory.js';
import { Status, type Config } from './index.js';

jest.mock('./checks/cache.js', () => ({
  cacheConnection: jest.fn(),
}));

jest.mock('./checks/cpu.js', () => ({
  cpuCheck: jest.fn(),
}));

jest.mock('./checks/db.js', () => ({
  dbConnection: jest.fn(),
}));

jest.mock('./checks/disk.js', () => ({
  disk: jest.fn(),
}));

jest.mock('./checks/egress.js', () => ({
  egress: jest.fn(),
}));

jest.mock('./checks/memory.js', () => ({
  memoryCheck: jest.fn(),
}));

describe('healthcheckHandler', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  const baseConfig = {
    checks: {
      memory_usage: false,
      outbound_internet: false,
      disk_space: false,
      cache_connection: false,
      db_connection: false,
      cpu_usage: false,
    },
  } as unknown as Config;

  it('should return a JSON response with pass status when no checks are enabled', async () => {
    const result = await healthcheckHandler({}, baseConfig);

    expect(result.type).toBe('application/json');
    const body = JSON.parse(result.body);
    expect(body).toEqual({
      status: Status.pass,
      description: 'Health check response, generated by allgood.',
      results: {},
    });
  });

  it('should return a JSON response by default when accept header is missing', async () => {
    const result = await healthcheckHandler({ accept: undefined }, baseConfig);

    expect(result.type).toBe('application/json');
  });

  it('should return a JSON response when accept header is application/json', async () => {
    const result = await healthcheckHandler({ accept: 'application/json' }, baseConfig);

    expect(result.type).toBe('application/json');
  });

  it('should run enabled checks and aggregate pass status', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue({
      componentName: 'memory',
      status: Status.pass,
      message: 'Memory usage is below 80%',
      value: '50%',
      time: 5,
    });

    (cpuCheck as jest.Mock).mockResolvedValue({
      componentName: 'cpu',
      status: Status.pass,
      message: 'CPU usage is below 80%',
      value: '30%',
      time: 3,
    });

    const config = {
      checks: {
        ...baseConfig.checks,
        memory_usage: true,
        cpu_usage: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect(body.status).toBe(Status.pass);
    expect(body.results).toEqual({
      memory_usage: {
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50%',
        time: 5,
      },
      cpu_usage: {
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU usage is below 80%',
        value: '30%',
        time: 3,
      },
    });

    expect(memoryCheck).toHaveBeenCalledWith(config);
    expect(cpuCheck).toHaveBeenCalledWith(config);
    expect(egress).not.toHaveBeenCalled();
    expect(disk).not.toHaveBeenCalled();
    expect(cacheConnection).not.toHaveBeenCalled();
    expect(dbConnection).not.toHaveBeenCalled();
  });

  it('should set overall status to fail when a check fails', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue({
      componentName: 'memory',
      status: Status.pass,
      message: 'Memory usage is below 80%',
      value: '50%',
      time: 5,
    });

    (dbConnection as jest.Mock).mockResolvedValue({
      componentName: 'db',
      status: Status.fail,
      message: 'DB connection failed',
      value: 'false',
      time: 10,
    });

    const config = {
      checks: {
        ...baseConfig.checks,
        memory_usage: true,
        db_connection: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect(body.status).toBe(Status.fail);
  });

  it('should set overall status to warn when a check warns and no failures occur', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue({
      componentName: 'memory',
      status: Status.warn,
      message: 'Memory usage is approaching limit',
      value: '85%',
      time: 5,
    });

    const config = {
      checks: {
        ...baseConfig.checks,
        memory_usage: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect(body.status).toBe(Status.warn);
  });

  it('should let a later fail override an earlier warn', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue({
      componentName: 'memory',
      status: Status.warn,
      message: 'Memory usage is approaching limit',
      value: '85%',
      time: 5,
    });

    (dbConnection as jest.Mock).mockResolvedValue({
      componentName: 'db',
      status: Status.fail,
      message: 'DB connection failed',
      value: 'false',
      time: 10,
    });

    const config = {
      checks: {
        ...baseConfig.checks,
        memory_usage: true,
        db_connection: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect([Status.fail, Status.warn]).toContain(body.status);
  });

  it('should skip checks not present in the checks registry', async () => {
    const config = {
      checks: {
        ...baseConfig.checks,
        unknown_check: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect(body.results).toEqual({});
    expect(body.status).toBe(Status.pass);
  });

  it('should skip checks that are disabled', async () => {
    const config = {
      checks: {
        ...baseConfig.checks,
        memory_usage: false,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);

    expect(memoryCheck).not.toHaveBeenCalled();
    const body = JSON.parse(result.body);
    expect(body.results).toEqual({});
  });

  it('should run all checks when all are enabled', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue({
      componentName: 'memory',
      status: Status.pass,
      message: 'Memory usage is below 80%',
      value: '50%',
      time: 1,
    });
    (egress as jest.Mock).mockResolvedValue({
      componentName: 'egress',
      status: Status.pass,
      message: 'Outbound internet is reachable',
      value: 'true',
      time: 1,
    });
    (disk as jest.Mock).mockResolvedValue({
      componentName: 'disk',
      status: Status.pass,
      message: 'Disk space is sufficient',
      value: '50%',
      time: 1,
    });
    (cacheConnection as jest.Mock).mockResolvedValue({
      componentName: 'cache_connection',
      status: Status.pass,
      message: 'Cache connection successful',
      value: 'true',
      time: 1,
    });
    (dbConnection as jest.Mock).mockResolvedValue({
      componentName: 'db',
      status: Status.pass,
      message: 'DB connection successful',
      value: 'true',
      time: 1,
    });
    (cpuCheck as jest.Mock).mockResolvedValue({
      componentName: 'cpu',
      status: Status.pass,
      message: 'CPU usage is below 80%',
      value: '30%',
      time: 1,
    });

    const config = {
      checks: {
        memory_usage: true,
        outbound_internet: true,
        disk_space: true,
        cache_connection: true,
        db_connection: true,
        cpu_usage: true,
      },
    } as unknown as Config;

    const result = await healthcheckHandler({}, config);
    const body = JSON.parse(result.body);

    expect(Object.keys(body.results).sort()).toEqual([
      'cache_connection',
      'cpu_usage',
      'db_connection',
      'disk_space',
      'memory_usage',
      'outbound_internet',
    ]);
    expect(body.status).toBe(Status.pass);
  });

  describe('HTML response', () => {
    it('should return an HTML response when accept header includes text/html', async () => {
      const result = await healthcheckHandler({ accept: 'text/html' }, baseConfig);

      expect(result.type).toBe('text/html');
      expect(result.body).toContain('<!DOCTYPE html>');
    });

    it('should render a pass banner when overall status is pass', async () => {
      const result = await healthcheckHandler({ accept: 'text/html' }, baseConfig);

      expect(result.body).toContain("👌 It's All Good");
      expect(result.body).toContain('#4CAF50');
    });

    it('should render a fail/warn banner when overall status is not pass', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.fail,
        message: 'Memory usage is critical',
        value: '95%',
        time: 2,
      });

      const config = {
        checks: {
          ...baseConfig.checks,
          memory_usage: true,
        },
      } as unknown as Config;

      const result = await healthcheckHandler({ accept: 'text/html' }, config);

      expect(result.body).toContain("❌ Something's Wrong");
      expect(result.body).toContain('#F44336');
    });

    it('should render a warn banner color when overall status is warn', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.warn,
        message: 'Memory usage is approaching limit',
        value: '85%',
        time: 2,
      });

      const config = {
        checks: {
          ...baseConfig.checks,
          memory_usage: true,
        },
      } as unknown as Config;

      const result = await healthcheckHandler({ accept: 'text/html' }, config);

      expect(result.body).toContain('#FFC107');
    });

    it('should list checks sorted alphabetically by check name', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory check message',
        value: '50%',
        time: 5,
      });
      (cpuCheck as jest.Mock).mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU check message',
        value: '30%',
        time: 3,
      });

      const config = {
        checks: {
          ...baseConfig.checks,
          memory_usage: true,