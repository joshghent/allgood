import { healthcheckHandler } from './healthcheck.js';
import { Status } from './index.js';
import { cacheConnection } from './checks/cache.js';
import { cpuCheck } from './checks/cpu.js';
import { dbConnection } from './checks/db.js';
import { disk } from './checks/disk.js';
import { egress } from './checks/egress.js';
import { memoryCheck } from './checks/memory.js';

jest.mock('./checks/cache.js');
jest.mock('./checks/cpu.js');
jest.mock('./checks/db.js');
jest.mock('./checks/disk.js');
jest.mock('./checks/egress.js');
jest.mock('./checks/memory.js');

describe('healthcheckHandler', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('JSON response', () => {
    it('should return JSON response when accept header is not text/html', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.type).toBe('application/json');
      expect(JSON.parse(result.body)).toEqual({
        status: Status.pass,
        description: 'Health check response, generated by allgood.',
        results: {
          memory_usage: {
            componentName: 'memory',
            status: Status.pass,
            message: 'Memory usage is below 80%',
            value: '50.00%',
            time: 10,
          },
        },
      });
    });

    it('should return JSON response when accept header is undefined', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        {},
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.type).toBe('application/json');
      expect(JSON.parse(result.body)).toMatchObject({
        status: Status.pass,
        description: 'Health check response, generated by allgood.',
      });
    });

    it('should return fail status when one check fails', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.fail,
        message: 'Memory usage is above 80%',
        value: '90.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      const body = JSON.parse(result.body);
      expect(body.status).toBe(Status.fail);
    });

    it('should return warn status when one check warns', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.warn,
        message: 'Memory usage is high',
        value: '75.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      const body = JSON.parse(result.body);
      expect(body.status).toBe(Status.warn);
    });

    it('should run multiple checks concurrently', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      (cpuCheck as jest.Mock).mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '40.00%',
        time: 15,
      });

      (disk as jest.Mock).mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '60.00%',
        time: 20,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
            cpu_usage: true,
            disk_space: true,
          },
        }
      );

      expect(memoryCheck).toHaveBeenCalled();
      expect(cpuCheck).toHaveBeenCalled();
      expect(disk).toHaveBeenCalled();

      const body = JSON.parse(result.body);
      expect(Object.keys(body.results)).toHaveLength(3);
      expect(body.results).toHaveProperty('memory_usage');
      expect(body.results).toHaveProperty('cpu_usage');
      expect(body.results).toHaveProperty('disk_space');
    });

    it('should only run enabled checks', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
            cpu_usage: false,
            disk_space: false,
          },
        }
      );

      expect(memoryCheck).toHaveBeenCalled();
      expect(cpuCheck).not.toHaveBeenCalled();
      expect(disk).not.toHaveBeenCalled();

      const body = JSON.parse(result.body);
      expect(Object.keys(body.results)).toHaveLength(1);
      expect(body.results).toHaveProperty('memory_usage');
    });

    it('should run all available checks when enabled', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      (cpuCheck as jest.Mock).mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '40.00%',
        time: 15,
      });

      (disk as jest.Mock).mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '60.00%',
        time: 20,
      });

      (cacheConnection as jest.Mock).mockResolvedValue({
        componentName: 'cache_connection',
        status: Status.pass,
        message: 'Cache OK',
        value: 'true',
        time: 25,
      });

      (dbConnection as jest.Mock).mockResolvedValue({
        componentName: 'db_connection',
        status: Status.pass,
        message: 'DB OK',
        value: 'true',
        time: 30,
      });

      (egress as jest.Mock).mockResolvedValue({
        componentName: 'outbound_internet',
        status: Status.pass,
        message: 'Egress OK',
        value: 'true',
        time: 35,
      });

      const result = await healthcheckHandler(
        { accept: 'application/json' },
        {
          checks: {
            memory_usage: true,
            cpu_usage: true,
            disk_space: true,
            cache_connection: true,
            db_connection: true,
            outbound_internet: true,
          },
          cache_connection: 'redis://localhost:6379',
          db_connection: 'postgres://localhost:5432',
        }
      );

      const body = JSON.parse(result.body);
      expect(Object.keys(body.results)).toHaveLength(6);
    });
  });

  describe('HTML response', () => {
    it('should return HTML response when accept header includes text/html', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.type).toBe('text/html');
      expect(result.body).toContain('<!DOCTYPE html>');
      expect(result.body).toContain('Health Check');
      expect(result.body).toContain('Memory usage is below 80%');
    });

    it('should show green banner for pass status', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.body).toContain('#4CAF50');
      expect(result.body).toContain('👌 It\'s All Good');
    });

    it('should show yellow banner for warn status', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.warn,
        message: 'Memory high',
        value: '75.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.body).toContain('#FFC107');
      expect(result.body).toContain('❌ Something\'s Wrong');
    });

    it('should show red banner for fail status', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.fail,
        message: 'Memory critical',
        value: '95.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.body).toContain('#F44336');
      expect(result.body).toContain('❌ Something\'s Wrong');
    });

    it('should display check results with appropriate icons', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      (cpuCheck as jest.Mock).mockResolvedValue({
        componentName: 'cpu',
        status: Status.fail,
        message: 'CPU critical',
        value: '95.00%',
        time: 15,
      });

      (disk as jest.Mock).mockResolvedValue({
        componentName: 'disk',
        status: Status.warn,
        message: 'Disk high',
        value: '75.00%',
        time: 20,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
            cpu_usage: true,
            disk_space: true,
          },
        }
      );

      expect(result.body).toContain('✅');
      expect(result.body).toContain('❌');
      expect(result.body).toContain('⚠️');
    });

    it('should hide boolean values in HTML display', async () => {
      (cacheConnection as jest.Mock).mockResolvedValue({
        componentName: 'cache_connection',
        status: Status.pass,
        message: 'Cache OK',
        value: 'true',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            cache_connection: true,
          },
          cache_connection: 'redis://localhost:6379',
        }
      );

      expect(result.body).toContain('Cache OK');
      expect(result.body).not.toContain('(true)');
    });

    it('should show non-boolean values in HTML display', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.body).toContain('(50.00%)');
    });

    it('should display time for each check', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 123,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
          },
        }
      );

      expect(result.body).toContain('[123ms]');
    });

    it('should sort checks alphabetically in HTML', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50.00%',
        time: 10,
      });

      (cpuCheck as jest.Mock).mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '40.00%',
        time: 15,
      });

      (disk as jest.Mock).mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '60.00%',
        time: 20,
      });

      const result = await healthcheckHandler(
        { accept: 'text/html' },
        {
          checks: {
            memory_usage: true,
            disk_space: true,
            cpu_usage: true,
          },
        }
      );

      const cpuIndex = result.body.indexOf('CPU OK');
      const diskIndex = result.body.indexOf('Disk OK');
      const memoryIndex = result.body.indexOf('Memory OK');

      expect(cpuIndex).toBeLessThan(diskIndex);
      expect(diskIndex).toBeLessThan(memoryIndex);
    });

    it('should handle accept header with multiple values including text/html', async () => {
      (memoryCheck as jest.Mock).mockResolvedValue({
        componentName: 'memory