import { healthcheckHandler } from './healthcheck.js';
import { Status } from './index.js';
import * as cacheModule from './checks/cache.js';
import * as cpuModule from './checks/cpu.js';
import * as dbModule from './checks/db.js';
import * as diskModule from './checks/disk.js';
import * as egressModule from './checks/egress.js';
import * as memoryModule from './checks/memory.js';

jest.mock('./checks/cache.js');
jest.mock('./checks/cpu.js');
jest.mock('./checks/db.js');
jest.mock('./checks/disk.js');
jest.mock('./checks/egress.js');
jest.mock('./checks/memory.js');

describe('healthcheckHandler', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('JSON response', () => {
    it('should return JSON response when accept header is not text/html', async () => {
      const mockMemoryCheck = jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50%',
        time: 10,
      });

      const headers = {
        accept: 'application/json',
      };

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.type).toBe('application/json');
      expect(JSON.parse(result.body)).toEqual({
        status: Status.pass,
        description: 'Health check response, generated by allgood.',
        results: {
          memory_usage: {
            componentName: 'memory',
            status: Status.pass,
            message: 'Memory usage is below 80%',
            value: '50%',
            time: 10,
          },
        },
      });
      expect(mockMemoryCheck).toHaveBeenCalledWith(config);
    });

    it('should return JSON response when accept header is undefined', async () => {
      const mockMemoryCheck = jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50%',
        time: 10,
      });

      const headers = {};

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.type).toBe('application/json');
      expect(JSON.parse(result.body)).toEqual({
        status: Status.pass,
        description: 'Health check response, generated by allgood.',
        results: {
          memory_usage: {
            componentName: 'memory',
            status: Status.pass,
            message: 'Memory usage is below 80%',
            value: '50%',
            time: 10,
          },
        },
      });
    });

    it('should return fail status when any check fails', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.fail,
        message: 'Memory usage is above 80%',
        value: '90%',
        time: 10,
      });

      const headers = {};

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.type).toBe('application/json');
      const body = JSON.parse(result.body);
      expect(body.status).toBe(Status.fail);
    });

    it('should return warn status when any check warns', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.warn,
        message: 'Memory usage is moderate',
        value: '75%',
        time: 10,
      });

      const headers = {};

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.type).toBe('application/json');
      const body = JSON.parse(result.body);
      expect(body.status).toBe(Status.warn);
    });
  });

  describe('HTML response', () => {
    it('should return HTML response when accept header includes text/html', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50%',
        time: 10,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.type).toBe('text/html');
      expect(result.body).toContain('<!DOCTYPE html>');
      expect(result.body).toContain('Health Check');
      expect(result.body).toContain('👌 It\'s All Good');
      expect(result.body).toContain('#4CAF50');
      expect(result.body).toContain('Memory usage is below 80%');
      expect(result.body).toContain('✅');
    });

    it('should display warn banner when status is warn', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.warn,
        message: 'Memory usage is moderate',
        value: '75%',
        time: 10,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.body).toContain('❌ Something\'s Wrong');
      expect(result.body).toContain('#FFC107');
    });

    it('should display fail banner when status is fail', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.fail,
        message: 'Memory usage is above 80%',
        value: '90%',
        time: 10,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.body).toContain('❌ Something\'s Wrong');
      expect(result.body).toContain('#F44336');
    });

    it('should display correct icons for different statuses', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50%',
        time: 10,
      });

      jest.spyOn(cpuModule, 'cpuCheck').mockResolvedValue({
        componentName: 'cpu',
        status: Status.warn,
        message: 'CPU moderate',
        value: '75%',
        time: 15,
      });

      jest.spyOn(diskModule, 'disk').mockResolvedValue({
        componentName: 'disk',
        status: Status.fail,
        message: 'Disk full',
        value: '95%',
        time: 20,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
          cpu_usage: true,
          disk_space: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.body).toContain('✅');
      expect(result.body).toContain('⚠️');
      expect(result.body).toContain('❌');
    });

    it('should not display value when it is "true" or "false"', async () => {
      jest.spyOn(cacheModule, 'cacheConnection').mockResolvedValue({
        componentName: 'cache_connection',
        status: Status.pass,
        message: 'Cache connection successful',
        value: 'true',
        time: 10,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          cache_connection: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.body).toContain('Cache connection successful');
      expect(result.body).not.toContain('(true)');
    });

    it('should display value when it is not "true" or "false"', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory usage is below 80%',
        value: '50%',
        time: 10,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(result.body).toContain('(50%)');
    });

    it('should display checks sorted alphabetically', async () => {
      jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50%',
        time: 10,
      });

      jest.spyOn(cpuModule, 'cpuCheck').mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '30%',
        time: 15,
      });

      jest.spyOn(diskModule, 'disk').mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '40%',
        time: 20,
      });

      const headers = {
        accept: 'text/html',
      };

      const config = {
        checks: {
          memory_usage: true,
          cpu_usage: true,
          disk_space: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      const cpuIndex = result.body.indexOf('CPU OK');
      const diskIndex = result.body.indexOf('Disk OK');
      const memoryIndex = result.body.indexOf('Memory OK');

      expect(cpuIndex).toBeLessThan(diskIndex);
      expect(diskIndex).toBeLessThan(memoryIndex);
    });
  });

  describe('multiple checks', () => {
    it('should run all enabled checks', async () => {
      const mockMemoryCheck = jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50%',
        time: 10,
      });

      const mockCpuCheck = jest.spyOn(cpuModule, 'cpuCheck').mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '30%',
        time: 15,
      });

      const mockDiskCheck = jest.spyOn(diskModule, 'disk').mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '40%',
        time: 20,
      });

      const headers = {};

      const config = {
        checks: {
          memory_usage: true,
          cpu_usage: true,
          disk_space: true,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(mockMemoryCheck).toHaveBeenCalledWith(config);
      expect(mockCpuCheck).toHaveBeenCalledWith(config);
      expect(mockDiskCheck).toHaveBeenCalledWith(config);

      const body = JSON.parse(result.body);
      expect(Object.keys(body.results)).toHaveLength(3);
    });

    it('should not run disabled checks', async () => {
      const mockMemoryCheck = jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50%',
        time: 10,
      });

      const mockCpuCheck = jest.spyOn(cpuModule, 'cpuCheck').mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '30%',
        time: 15,
      });

      const headers = {};

      const config = {
        checks: {
          memory_usage: true,
          cpu_usage: false,
        },
      };

      const result = await healthcheckHandler(headers, config);

      expect(mockMemoryCheck).toHaveBeenCalledWith(config);
      expect(mockCpuCheck).not.toHaveBeenCalled();

      const body = JSON.parse(result.body);
      expect(Object.keys(body.results)).toHaveLength(1);
      expect(body.results.memory_usage).toBeDefined();
      expect(body.results.cpu_usage).toBeUndefined();
    });

    it('should run all check types', async () => {
      const mockMemoryCheck = jest.spyOn(memoryModule, 'memoryCheck').mockResolvedValue({
        componentName: 'memory',
        status: Status.pass,
        message: 'Memory OK',
        value: '50%',
        time: 10,
      });

      const mockCpuCheck = jest.spyOn(cpuModule, 'cpuCheck').mockResolvedValue({
        componentName: 'cpu',
        status: Status.pass,
        message: 'CPU OK',
        value: '30%',
        time: 15,
      });

      const mockDiskCheck = jest.spyOn(diskModule, 'disk').mockResolvedValue({
        componentName: 'disk',
        status: Status.pass,
        message: 'Disk OK',
        value: '40%',
        time: 20,
      });

      const mockCacheCheck = jest.spyOn(cacheModule, 'cacheConnection').mockResolvedValue({
        componentName: 'cache_connection',
        status: Status.pass,
        message: 'Cache OK',
        value: 'true',
        time: 25,
      });

      const mockDbCheck = jest.spyOn(dbModule, 'dbConnection').mockResolvedValue({
        componentName: 'db_connection',
        status: Status.pass,
        message: 'DB OK',
        value: 'true',
        time: 30,
      });

      const mockEgressCheck = jest.spyOn(egressModule, 'egress').mockResolvedValue({
        componentName: 'outbound_internet',
        status: Status.pass,
        message: 'Egress OK',
        value: 'true',
        time: 35,
      });