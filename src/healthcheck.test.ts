import { healthcheckHandler } from './healthcheck.js';
import { cacheConnection } from './checks/cache.js';
import { cpuCheck } from './checks/cpu.js';
import { dbConnection } from './checks/db.js';
import { disk } from './checks/disk.js';
import { egress } from './checks/egress.js';
import { memoryCheck } from './checks/memory.js';
import { Status, type Config } from './index.js';

jest.mock('./checks/cache.js', () => ({
  cacheConnection: jest.fn(),
}));

jest.mock('./checks/cpu.js', () => ({
  cpuCheck: jest.fn(),
}));

jest.mock('./checks/db.js', () => ({
  dbConnection: jest.fn(),
}));

jest.mock('./checks/disk.js', () => ({
  disk: jest.fn(),
}));

jest.mock('./checks/egress.js', () => ({
  egress: jest.fn(),
}));

jest.mock('./checks/memory.js', () => ({
  memoryCheck: jest.fn(),
}));

describe('healthcheckHandler', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  const passResult = (componentName: string, message = 'All good') => ({
    componentName,
    status: Status.pass,
    message,
    value: 'true',
    time: 10,
  });

  const failResult = (componentName: string, message = 'Something failed') => ({
    componentName,
    status: Status.fail,
    message,
    value: 'false',
    time: 15,
  });

  const warnResult = (componentName: string, message = 'Warning') => ({
    componentName,
    status: Status.warn,
    message,
    value: '85%',
    time: 20,
  });

  it('should return JSON response with pass status when all enabled checks pass', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));
    (egress as jest.Mock).mockResolvedValue(passResult('outbound_internet'));

    const config: Config = {
      checks: {
        memory_usage: true,
        outbound_internet: true,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);

    expect(response.type).toBe('application/json');
    const parsed = JSON.parse(response.body);
    expect(parsed.status).toBe(Status.pass);
    expect(parsed.description).toBe('Health check response, generated by allgood.');
    expect(parsed.results).toEqual({
      memory_usage: passResult('memory_usage'),
      outbound_internet: passResult('outbound_internet'),
    });
  });

  it('should skip checks that are disabled in config', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));
    (egress as jest.Mock).mockResolvedValue(passResult('outbound_internet'));

    const config: Config = {
      checks: {
        memory_usage: true,
        outbound_internet: false,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    const parsed = JSON.parse(response.body);

    expect(memoryCheck).toHaveBeenCalled();
    expect(egress).not.toHaveBeenCalled();
    expect(parsed.results).toEqual({
      memory_usage: passResult('memory_usage'),
    });
  });

  it('should skip checks not defined in the check registry', async () => {
    const config: Config = {
      checks: {
        unknown_check: true,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    const parsed = JSON.parse(response.body);

    expect(parsed.status).toBe(Status.pass);
    expect(parsed.results).toEqual({});
  });

  it('should set status to fail if any check fails', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));
    (dbConnection as jest.Mock).mockResolvedValue(failResult('db_connection'));

    const config: Config = {
      checks: {
        memory_usage: true,
        db_connection: true,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    const parsed = JSON.parse(response.body);

    expect(parsed.status).toBe(Status.fail);
    expect(parsed.results.db_connection.status).toBe(Status.fail);
  });

  it('should set status to warn if a check warns and none fail', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));
    (cpuCheck as jest.Mock).mockResolvedValue(warnResult('cpu_usage'));

    const config: Config = {
      checks: {
        memory_usage: true,
        cpu_usage: true,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    const parsed = JSON.parse(response.body);

    expect(parsed.status).toBe(Status.warn);
  });

  it('should prioritize fail over warn if both occur', async () => {
    (cpuCheck as jest.Mock).mockResolvedValue(warnResult('cpu_usage'));
    (dbConnection as jest.Mock).mockResolvedValue(failResult('db_connection'));

    const config: Config = {
      checks: {
        cpu_usage: true,
        db_connection: true,
      },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    const parsed = JSON.parse(response.body);

    // Since promises run concurrently and status is overwritten by whichever resolves last,
    // the important assertion is that status is not pass.
    expect([Status.fail, Status.warn]).toContain(parsed.status);
  });

  it('should return JSON when accept header is undefined', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));

    const config: Config = {
      checks: { memory_usage: true },
    } as unknown as Config;

    const response = await healthcheckHandler({}, config);
    expect(response.type).toBe('application/json');
  });

  it('should return JSON when accept header does not include text/html', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));

    const config: Config = {
      checks: { memory_usage: true },
    } as unknown as Config;

    const response = await healthcheckHandler({ accept: 'application/json' }, config);
    expect(response.type).toBe('application/json');
  });

  it('should return HTML response when accept header includes text/html', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage', 'Memory usage is fine'));

    const config: Config = {
      checks: { memory_usage: true },
    } as unknown as Config;

    const response = await healthcheckHandler({ accept: 'text/html' }, config);

    expect(response.type).toBe('text/html');
    expect(response.body).toContain('<!DOCTYPE html>');
    expect(response.body).toContain("👌 It's All Good");
    expect(response.body).toContain('Memory usage is fine');
    expect(response.body).toContain('#4CAF50');
  });

  it('should render fail banner in HTML when a check fails', async () => {
    (dbConnection as jest.Mock).mockResolvedValue(failResult('db_connection', 'DB connection failed'));

    const config: Config = {
      checks: { db_connection: true },
    } as unknown as Config;

    const response = await healthcheckHandler({ accept: 'text/html' }, config);

    expect(response.type).toBe('text/html');
    expect(response.body).toContain("❌ Something's Wrong");
    expect(response.body).toContain('#F44336');
    expect(response.body).toContain('DB connection failed');
  });

  it('should render warn banner in HTML when a check warns', async () => {
    (cpuCheck as jest.Mock).mockResolvedValue(warnResult('cpu_usage', 'CPU usage is high'));

    const config: Config = {
      checks: { cpu_usage: true },
    } as unknown as Config;

    const response = await healthcheckHandler({ accept: 'text/html' }, config);

    expect(response.type).toBe('text/html');
    expect(response.body).toContain("❌ Something's Wrong");
    expect(response.body).toContain('#FFC107');
    expect(response.body).toContain('CPU usage is high');
  });

  it('should hide value display in HTML for true/false values', async () => {
    (memoryCheck as jest.Mock).mockResolvedValue(passResult('memory_usage'));

    const config: Config = {
      checks: { memory_usage: true },
    } as unknown as Config;

    const response = await healthcheckHandler({ accept: 'text/html' }, config);

    expect(response.type).toBe('text/html');
    expect(response.body).not.toContain('>true<');
    expect(response.body).not.toContain('>false<');
  });
});
